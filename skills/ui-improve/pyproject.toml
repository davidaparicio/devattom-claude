[project]
name = "ui-improve"
version = "1.0.0"
description = "Iterative UI analysis and improvement via Gemini Vision"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.0.0",
    "python-dotenv>=1.0.0",
    "Pillow>=10.0.0",
]
